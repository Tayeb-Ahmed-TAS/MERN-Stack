# SQL (Structured Query Language)

**NOTE:** **MYSQL** is a DataBase and **SQL** is a Language to interact with the DataBase.

So, SQL is not a Database itself. It's a language used to manage and manipulate databases.
