# Export Files in Node.js

In `require()` statement, just write the path of the file. Like,

```javascript
const myModule = require("./myModule");
```

Here, `./myModule` is the path of the file you want to import.

`./` means the current directory.
